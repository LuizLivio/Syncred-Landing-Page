# URL da API do backend
# Para desenvolvimento local: deixe vazio (usa proxy do Vite)
# Para produção: URL completa do backend (ex: https://seu-id.ngrok-free.app)
# Nota: Em dev local, o proxy do Vite redireciona /api para http://localhost:3001
VITE_API_URL=
